import { extendTheme } from "@chakra-ui/react";

const Theme = extendTheme({
  colors: {
    primary: "#FF5733",
    secondary: "#007BFF",

    // Backgrounds
    bgA: "#240046ef",
    bgB: "#59189adf",
  },
});

export default Theme;
